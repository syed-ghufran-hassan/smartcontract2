pragma solidity ^0.8.0;

contract Onlinebookmarketplace {
struct buyer { 
        uint desiredPrice;
    }
struct seller{   
        uint demandPrice;
    }
buyer[] public buy;
seller[] public sell;
 
function dealer(uint memory desiredPrice, uint demandPrice) public view return () 
{      
    buy.push(desiredPrice);
    sell.push(demandPrice);
    if (desiredPrice == demandPrice) 
     
}
}
